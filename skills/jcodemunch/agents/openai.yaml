interface:
  display_name: "jCodeMunch"
  short_description: "Enforce jCodeMunch code navigation"
  default_prompt: "Use $jcodemunch to navigate repository source under its required jCodeMunch-only exploration policy."

dependencies:
  tools:
    - type: "mcp"
      value: "jcodemunch"
      description: "jCodeMunch MCP server for symbol-level repository retrieval"

policy:
  allow_implicit_invocation: false
