interface:
  display_name: "jCodeMunch"
  short_description: "Enforce jCodeMunch code navigation"
  default_prompt: "Use $jcodemunch to navigate this repository exclusively through jCodeMunch before making changes."

dependencies:
  tools:
    - type: "mcp"
      value: "jcodemunch"
      description: "jCodeMunch MCP server for symbol-level repository retrieval"
