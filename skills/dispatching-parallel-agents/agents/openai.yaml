interface:
  display_name: "Dispatching Parallel Agents"
  short_description: "Split independent work across Codex subagents"
  default_prompt: "Use $dispatching-parallel-agents to partition this task into independent workstreams, coordinate subagents, and synthesize verified results."
