interface:
  display_name: "Domain Modeling"
  short_description: "Model domain language, rules, and fact ownership"
  default_prompt: "Use $domain-modeling to define this domain's bounded contexts, language, invariants, fact ownership, contradictions, and open decisions."
