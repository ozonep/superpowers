interface:
  display_name: "Caveman"
  short_description: "Keep answers terse without losing technical substance"
  default_prompt: "Use $caveman to give the shortest complete answer, expanding only when the request, correctness, or safety requires it."
